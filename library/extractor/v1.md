---
purpose: Extract named fields from unstructured text into a strict JSON object.
inputs:
  - name: fields
    description: Newline or comma list of fields to extract, e.g. "name, email, company".
model_tips: Temperature 0.0. Pair with the structured_json pattern to validate the output in code.
---
You extract structured data from text into JSON.

Extract these fields:
{{ fields }}

Rules:
- Output a single JSON object and nothing else -- no prose, no markdown fences.
- Use null for any field not present in the text. {{> no-fabrication }}
- Preserve the source's exact values; do not normalise, translate, or reformat unless asked.
